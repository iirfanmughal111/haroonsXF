{
    "template": "post_macros",
    "description": "Removes reply link from TV thread",
    "execution_order": 10,
    "enabled": true,
    "action": "preg_replace",
    "find": "/<xf:if is=\"\\$thread.canReply().+xf:if>.+xf:if>/isU",
    "replace": "<xf:if is=\"$thread.traktTV is empty || !$post.isFirstPost()\">\n\t$0\n</xf:if>"
}