{
    "template": "forum_post_thread",
    "description": "fs_auction_post_thread_add_default_prefix_id",
    "execution_order": 10,
    "enabled": true,
    "action": "str_replace",
    "find": "prefix-value=\"{{ $forum.draft_thread.prefix_id ?: ($thread.prefix_id ?: $forum.default_prefix_id) }}\"",
    "replace": "prefix-value=\"{{ $forum.node_id == $xf.options.fs_auction_applicable_forum ? $xf.options.auction_thread_prefix_id : ($forum.draft_thread.prefix_id ?: ($thread.prefix_id ?: $forum.default_prefix_id)) }}\""
}